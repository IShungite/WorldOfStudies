{
  "name": "world-of-studies",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "pre-commit": "lint-staged",
    "lint:all": "pnpm run -r lint",
    "test:all": "pnpm run -r test",
    "start": "docker compose up"
  },
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.0.10",
    "lint-staged": "^15.2.1"
  }
}
